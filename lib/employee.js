//inquirer

// The first class is an `Employee` parent class with the following properties and methods:
// Employee will be a constructor and then inquirer will create a new instance based on the inputted information. 
// let Employee()
// .prompt
// name
// Employee id
// email
// Methods in the constructor
// getName()
// getId()
// getEmail()
// getRole();
//returns 'Employee';

// may want to make them all the same // google classes js https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes

// The other three classes will extend `Employee`. -- not prototype
// In addition to `Employee`'s properties and methods, `Engineer` will also have the following:

// github;
// GitHub username //'https://github.com/' + githubUserName

// getGithub()

// getRole();
//overridden to return 'Engineer'

// The other three classes will extend `Employee`. -- prototype

// In addition to `Employee`'s properties and methods, `Intern` will also have the following:

// school
// getSchool()

// getRole();.
//overridden to return 'Intern'

// The other three classes will extend `Employee`. -- prototype

// In addition to `Employee`'s properties and methods, `Manager` will also have the following:how to 

// officeNumber

//getRole();
//overridden to return 'Manager'